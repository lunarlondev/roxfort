const testBox = document.getElementById("js-test");

if (testBox) {
    testBox.innerHTML = "✅ A külső JavaScript sikeresen betöltődött!";
    testBox.style.background = "#26734d";
    testBox.style.fontSize = "20px";
}